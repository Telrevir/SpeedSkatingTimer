# DetectOnly LoRa 字节通信协议

本文档供APP端开发使用，定义APP与DetectOnly设备之间的LoRa字节通信协议。

## 一、数据包格式

所有数据包统一使用以下结构：

```text
包头 + 命令 ID + Payload 长度 + Payload + 校验和 + 包尾
```

字段定义：

```text
包头：       1 字节，固定为 0xAA
命令 ID：    1 字节
Payload长度：1 字节，只表示 Payload 的字节数
Payload：    0～230 字节，具体格式由命令 ID 决定
校验和：     1 字节
包尾：       1 字节，固定为 0xF9
```

校验和计算范围为“命令 ID + Payload 长度 + Payload”，取所有字节累加结果的低8位：

```text
checksum = (commandId + payloadLength + payload[0] + ... + payload[n]) & 0xFF
```

多字节整数统一采用大端序，即高位字节在前。

## 二、通用数据类型

### 1. EPC

EPC固定使用4字节原始数据。

例如文本EPC `3333F337`表示为：

```text
0x33 0x33 0xF3 0x37
```

### 2. 总时长

总时长表示从最近一次成功执行`0x01`开始命令，到EPC被有效检测时经过的时间。

总时长使用3字节无符号整数，单位为百分秒，即0.01秒：

```text
byte0：百分秒高位
byte1：百分秒中位
byte2：百分秒低位
```

最大值为`0xFFFFFF`，达到最大值后保持该值，不回绕。

### 3. 比赛状态

比赛状态使用1字节表示：

```text
0x00：已停止
0x01：检测中
```

固件开机后的默认状态为`0x00`。

## 三、命令列表

协议只保留下列6个命令 ID，其他命令均视为未知命令。

```text
0x01：开始检测
0x02：结束检测
0x03：获取比赛状态
0x04：返回比赛状态
0x13：检测到EPC
0xF0：返回执行状态
```

### 1. `0x01` 开始检测

发送方：APP

Payload长度：0

功能：开始一次新的EPC检测过程。执行结果通过`0xF0`返回。

如果当前已经处于检测中，则设备不执行任何操作，原有总时长和EPC去重状态保持不变，并通过`0xF0`返回“当前状态不允许执行”。

示例：

```text
AA 01 00 01 F9
```

### 2. `0x02` 结束检测

发送方：APP

Payload长度：0

功能：立即结束当前EPC检测过程，并通过`0xF0`返回执行结果。结束成功后，比赛状态变为`0x00`，下次开始时总时长从零计算。

如果当前已经处于停止状态，则不执行任何结束操作，并通过`0xF0`返回“当前状态不允许执行”。

示例：

```text
AA 02 00 02 F9
```

### 3. `0x03` 获取比赛状态

发送方：APP

Payload长度：0

功能：查询STM32当前比赛状态。请求有效时，STM32只返回`0x04`，不额外返回`0xF0`。

如果Payload长度错误，则STM32通过`0xF0`返回协议错误。

示例：

```text
AA 03 00 03 F9
```

### 4. `0x04` 返回比赛状态

发送方：STM32

Payload长度：1

Payload格式：

```text
byte0：比赛状态，0x00表示已停止，0x01表示检测中
```

示例：返回检测中状态。

```text
AA 04 01 01 06 F9
```

### 5. `0x13` 检测到EPC

发送方：STM32

Payload长度：7

Payload格式：

```text
byte0-byte3：EPC原始4字节
byte4-byte6：从最近一次成功开始检测起计算的总时长，uint24百分秒
```

示例：检测到EPC `3333F337`，总时长为100百分秒，即1.00秒。

```text
Payload：33 33 F3 37 00 00 64
完整包：AA 13 07 33 33 F3 37 00 00 64 0E F9
```

同一EPC距离上次有效检测不足8秒时，设备不会发送`0x13`或`0xF0`。达到或超过8秒后，设备才会再次发送该EPC。不同EPC互不影响。

### 6. `0xF0` 返回执行状态

发送方：STM32

Payload长度：2

Payload格式：

```text
byte0：触发本次返回的原始命令ID
byte1：状态码
```

状态码定义：

```text
0x00：成功
0x01：Payload长度错误
0x02：未知命令
0x03：RFID未初始化
0x04：RFID启动失败
0x05：RFID停止失败
0x06：EPC格式错误
0x07：RFID接收队列溢出
0x08：EPC去重表已满
0x09：LoRa发送失败
0x0A：当前状态不允许执行
0x0B：数据包校验和错误
0x0C：数据包尾错误
0x0D：Payload长度超过上限
```

示例：`0x01`开始检测成功。

```text
AA F0 02 01 00 F3 F9
```

示例：检测中重复收到`0x01`，不执行任何开始操作。

```text
AA F0 02 01 0A FD F9
```

收到未知命令时返回：

```text
byte0：收到的未知命令ID
byte1：0x02
```

## 四、命令响应关系

```text
APP发送0x01 → STM32返回0xF0
APP发送0x02 → STM32返回0xF0
APP发送0x03 → STM32返回0x04
STM32有效检测EPC → STM32主动发送0x13
STM32发现协议或运行错误 → STM32发送0xF0
```
