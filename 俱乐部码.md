# 俱乐部标识码保存/识别功能说明

## 一、功能目标

新增“俱乐部标识码”功能，用于给当前 STM32 计时主机保存一个固定的俱乐部身份标识。该标识码写入 STM32 内部 Flash，断电后保留；系统每次开机时自动读取，并在需要识别设备归属、区分俱乐部数据或参与后续通信协议扩展时使用。

该功能不依赖 TF 卡，不跟随 `athletes.csv` 变化，属于设备级配置。

## 二、标识码格式

俱乐部标识码固定为 2 位：

```text
<省份码><俱乐部码>
```

字段含义：

```text
第 1 位：省份码
第 2 位：俱乐部码
```

示例：

```text
A1
3F
BJ
```

建议字符范围：

```text
0-9
A-Z
```

输入时统一转为大写保存。小写输入允许，但保存前会转换为大写。

非法格式：

```text
A       长度不足
A12     长度过长
A-      包含非法字符
中1     非 ASCII 标识字符
```

## 三、存储方式

俱乐部标识码必须写入 STM32 内部 Flash。

建议单独建立配置管理模块：

```text
ClubCodeManager.h
```

建议 Flash 中保存以下结构：

```text
magic       固定标记，用于判断 Flash 中是否存在有效俱乐部码
version     配置版本号，便于后续升级
province    省份码，1 字节
club        俱乐部码，1 字节
checksum    简单校验值
```

推荐默认值：

```text
未设置
```

当 Flash 中没有有效数据、magic 不匹配或 checksum 校验失败时，系统应认为俱乐部码未设置，而不是使用随机 Flash 内容。

## 四、开机读取流程

系统启动时，在 `setup()` 中完成俱乐部码读取。

建议流程：

```text
1. 初始化串口。
2. 初始化俱乐部码管理模块。
3. 从 Flash 读取俱乐部码配置。
4. 校验 magic、version、checksum 和码值格式。
5. 如果有效，将俱乐部码加载到内存。
6. 如果无效，内存状态标记为“未设置”。
7. 串口打印当前俱乐部码状态。
```

串口日志示例：

```text
[ClubCode] 已读取俱乐部码: A1
[ClubCode] 俱乐部码未设置
[ClubCode] Flash 数据校验失败，已忽略
```

## 五、串口字符串命令

俱乐部码通过 USB 串口字符串命令写入。

### 1. 设置俱乐部码

命令格式：

```text
CLUB,SET,<code>
```

示例：

```text
CLUB,SET,A1
CLUB,SET,b7
```

处理规则：

```text
1. 解析第三段为俱乐部码。
2. trim 去除首尾空白。
3. 转为大写。
4. 校验长度必须为 2。
5. 校验每一位必须为 0-9 或 A-Z。
6. 写入 Flash。
7. 写入成功后更新内存中的俱乐部码。
```

成功日志：

```text
[ClubCode] 俱乐部码已保存: A1
```

失败日志：

```text
[ClubCode] 命令格式错误: CLUB,SET,<code>
[ClubCode] 俱乐部码格式错误，应为 2 位 0-9/A-Z
[ClubCode] Flash 写入失败
```

### 2. 查询俱乐部码

命令格式：

```text
CLUB,GET
```

返回示例：

```text
[ClubCode] 当前俱乐部码: A1
[ClubCode] 当前俱乐部码未设置
```

### 3. 清除俱乐部码

命令格式：

```text
CLUB,CLEAR
```

处理规则：

```text
1. 清除 Flash 中的有效标记，或写入“未设置”状态。
2. 清空内存中的俱乐部码。
3. 串口提示清除结果。
```

返回示例：

```text
[ClubCode] 俱乐部码已清除
```

该命令不是基础必需项，但建议保留，方便调试和设备转场使用。

## 六、识别逻辑

系统运行时应提供以下能力：

```text
是否已设置俱乐部码
获取完整 2 位俱乐部码
获取省份码
获取俱乐部码
判断输入俱乐部码是否匹配本机
```

建议接口：

```cpp
bool isClubCodeSet();
String getClubCode();
char getProvinceCode();
char getClubCodeChar();
bool matchClubCode(const String& code);
```

识别规则：

```text
1. 未设置俱乐部码时，任何外部俱乐部码都不应判定为匹配。
2. 比较前统一 trim 并转大写。
3. 只有完整 2 位码完全一致时才判定匹配。
```

## 七、与现有系统的关系

俱乐部码是设备配置，不属于运动员数据。

不应写入：

```text
athletes.csv
```

不应跟随以下操作清空：

```text
RESET 比赛
清空比赛计时数据
重新加载运动员列表
```

可以被以下操作影响：

```text
CLUB,SET,<code>
CLUB,CLEAR
后续如增加“恢复出厂设置”命令，也可以清除俱乐部码
```

## 八、异常处理要求

### 1. Flash 无有效数据

系统启动正常，俱乐部码状态为未设置。

### 2. Flash 校验失败

不使用 Flash 中的数据，状态置为未设置，并通过串口提示。

### 3. 写入失败

保留原内存俱乐部码，不应把内存状态更新为新值。

### 4. 非法串口命令

只打印错误提示，不修改 Flash，也不修改内存。

## 九、后续开发建议

建议后续代码实装分三步：

1. 新增 `ClubCodeManager.h`，封装 Flash 读写、格式校验和内存状态。
2. 在 `SpeedSkatingTimer.ino` 中新增全局对象，并在 `setup()` 开机读取。
3. 在串口命令处理中新增 `CLUB,SET,<code>`、`CLUB,GET`、`CLUB,CLEAR`。

如果后续需要让 ESP32 或上位机识别设备所属俱乐部，可以再扩展 LoRa 协议，增加“获取俱乐部码”和“返回俱乐部码”命令；当前需求只要求串口字符串命令写入，因此 LoRa 协议暂不修改。
