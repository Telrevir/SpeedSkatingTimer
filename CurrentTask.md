# 当前任务

## 已完成

- 在`DetectOnly`分支保留开始、结束和状态查询。
- 增加`0x10`定义EPC、`0x11`查询运动员、`0x12`运动员信息、`0x13`传输状态和`0x14`普通EPC。
- 使用四张各50槽的固定内存表维护运动员、运动员成绩、黑名单和普通EPC 8秒记录。
- 第一名运动员定义时启动运动员成绩时钟，圈数、单圈时长和总时长为0，后续运动员共用该成绩时钟。
- `0x12`返回运动员ID、圈数、单圈时长和总时长。
- 运动员和普通EPC分别执行8秒静默规则。
- 成功定义运动员时先返回`0xF0`成功，再立即返回`0x12`。
- `0x02`成功后清空所有临时比赛数据。
- 保持无TF卡、无数据库、无动态运动员存储。

## 待办

### RSSI 峰值计分

- [x] 已确认方案：保留即时计分保险路径，在`config.h`中选择模式，默认启用300ms内最强RSSI样本计分。
- [x] 已实现通知帧RSSI提取、50项固定窗口聚合和新旧模式分派；`RssiPeak`默认启用，`LegacyImmediate`保留为保险路径。
- [x] 已完成RSSI窗口与DetectionController集成的纯C++自测。
- [ ] 完成Arduino编译及STM32实机新旧模式对照；实机验收前不得标记完成。

### 近10圈历史与名次查询

- [x] 已确认设计：`docs/superpowers/specs/2026-09-18-ten-lap-history-ranking-design.md`；本任务替代未实现的100圈分片重连同步方案。
- [ ] 为每名运动员增加10条、每条7字节的固定环形历史，记录圈数、名次和总用时。
- [ ] 在每次有效过线后按圈数、总用时、有效扫描时间计算名次并写入历史。
- [ ] 扩展`0x11`传输序列，新增STM32返回的`0x15`近10圈历史包。
- [ ] 小程序解码`0x15`并按运动员ID替换近10圈列表。
- [ ] 同步维护`LORAProtocol-byte.md`、`ARCHITECTURE.md`及小程序协议文档。
- [ ] 完成固件自测、小程序自动化测试、STM32及小程序实机联调。

### 后端权威比赛存储与实时查询

- [x] 完成并确认总体设计：`docs/superpowers/specs/2026-09-07-server-backed-race-storage-design.md`。
- [x] 编写后端实施计划：`docs/superpowers/plans/2026-09-07-backend-race-storage-api.md`。
- [x] 编写小程序实施计划：`docs/superpowers/plans/2026-09-07-miniprogram-server-race-sync.md`。
- [ ] 后端优先完成协议、数据库迁移、比赛写入、分页和实时查询接口；暂不推送后端。
- [ ] 小程序依据后端最终协议完成接口隔离、缓存同步、Worker调度、比赛生命周期、历史分页和实时比赛页面。
- [x] 小程序 Task 4：完成版本化比赛工作副本、稳定比赛/成绩身份、活动会话身份恢复和持久 outbox；尚未接入网络、Worker 或比赛控制器。
- [ ] 协同验收数据库升级SQL、自动化测试、微信开发者工具及实机行为。

## 待实机验证

- 小程序自动补圈需使用真实比赛数据验证：正常圈不补、约2圈/3圈间隔分别补1/2圈、慢圈不误补、补圈偏移跨前后台和进程恢复、结束圈跨越不超圈。
- APP按`LORAProtocol-byte.md`完成`0x10`至`0x14`联调。
- 第一名和后续运动员定义后的9字节初始`0x12`内容。
- 运动员与普通EPC在8秒边界前后的发送行为。
- `0x11`空名单和50人名单的开始、条目、结束序列。
- RSSI峰值模式：验证同一运动员300ms内多次读数只生成一条`0x12`，且其单圈和总时长使用最高RSSI样本的接收时间；再切换`LegacyImmediate`重复验证即时保险路径。
- `0x02`后重新开始时，旧运动员和黑名单均不再生效。
