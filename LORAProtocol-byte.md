# DetectOnly LoRa 字节通信协议

本文档是 APP 对接 DetectOnly 固件的唯一有效协议说明。

## 一、数据包格式

所有数据包统一使用以下结构：

```text
包头 + 命令ID + Payload长度 + Payload + 校验和 + 包尾
```

```text
包头：          1字节，固定为0xAA
命令ID：        1字节
Payload长度：   1字节，只表示Payload字节数
Payload：       0～230字节
校验和：        1字节
包尾：          1字节，固定为0xF9
```

校验和计算范围为“命令ID + Payload长度 + Payload”，取累加结果低8位：

```text
checksum = (commandId + payloadLength + payload[0] + ... + payload[n]) & 0xFF
```

多字节整数统一使用大端序，高位字节在前。

## 二、通用数据类型

### EPC

EPC固定使用4字节原始数据。例如文本EPC `3333F337`表示为：

```text
33 33 F3 37
```

### 运动员ID

运动员ID使用2字节无符号大端整数。

### 圈数

圈数使用1字节无符号整数。

### 百分秒总时长

总时长使用3字节无符号大端整数，单位为百分秒，即0.01秒：

```text
byte0：高位
byte1：中位
byte2：低位
```

比赛时钟在成功执行`0x01`后从0开始。定义第一名运动员时记录当前比赛时钟为统一偏移量，第一名运动员的初始总时长固定为0。所有运动员此后的总时长均为：

```text
当前比赛时钟 - 统一偏移量
```

### 比赛状态

```text
0x00：已停止
0x01：检测中
```

固件开机后的默认状态为`0x00`。

## 三、命令列表

```text
0x01：开始检测
0x02：结束检测
0x03：获取比赛状态
0x04：返回比赛状态
0x10：定义EPC
0x11：获取所有运动员信息
0x12：返回运动员信息
0x13：运动员传输状态
0x14：检测到普通EPC
0xF0：返回执行状态
```

### 1. `0x01` 开始检测

发送方：APP

Payload长度：0

开始一场新的检测并启动比赛时钟。执行结果通过`0xF0`返回。检测中重复发送时不执行任何操作，原有时钟和内存数据保持不变，并返回“当前状态不允许执行”。

```text
AA 01 00 01 F9
```

### 2. `0x02` 结束检测

发送方：APP

Payload长度：0

立即结束当前检测。成功后停止比赛时钟，并清空运动员名单、EPC黑名单、普通EPC 8秒临时列表、运动员8秒记录、统一时间偏移量和RFID接收队列。执行结果通过`0xF0`返回。

停止状态下重复发送时不执行任何操作，并返回“当前状态不允许执行”。

```text
AA 02 00 02 F9
```

### 3. `0x03` 获取比赛状态

发送方：APP

Payload长度：0

请求有效时，STM32只返回`0x04`，不额外返回成功`0xF0`。

```text
AA 03 00 03 F9
```

### 4. `0x04` 返回比赛状态

发送方：STM32

Payload长度：1

```text
byte0：0x00表示已停止，0x01表示检测中
```

检测中示例：

```text
AA 04 01 01 06 F9
```

### 5. `0x10` 定义EPC

发送方：APP

Payload长度：7

```text
byte0：定义结果，0x01表示运动员，0x00表示非运动员
byte1-byte4：EPC
byte5-byte6：运动员ID；定义为非运动员时忽略该字段
```

该命令只允许在检测中执行。

定义为运动员时：

1. 将EPC、运动员ID、圈数和总时长写入临时运动员名单。
2. 第一名运动员圈数为0、总时长固定为0，并确定统一时间偏移量。
3. 后续运动员圈数为0，总时长为“当前比赛时钟－统一偏移量”。
4. 从普通EPC 8秒临时列表移除该EPC。
5. 先发送`0xF0`表示`0x10`执行成功，再立即发送该运动员的`0x12`。

定义为非运动员时，将EPC加入黑名单、从普通EPC 8秒临时列表移除，并只发送成功`0xF0`。黑名单EPC后续检测完全静默。

定义运动员示例：EPC为`3333F337`，运动员ID为1。

```text
Payload：01 33 33 F3 37 00 01
完整包：AA 10 07 01 33 33 F3 37 00 01 A9 F9
```

### 6. `0x11` 获取所有运动员信息

发送方：APP

Payload长度：0

该命令只允许在检测中执行。收到有效请求后，STM32按以下顺序发送：

```text
0x13：开始传输
0x12：第一名启用的运动员
0x12：第二名启用的运动员
...
0x13：结束传输
```

空名单仍发送开始和结束。正常传输不额外返回成功`0xF0`。

```text
AA 11 00 11 F9
```

### 7. `0x12` 返回运动员信息

发送方：STM32

Payload长度：6

```text
byte0-byte1：运动员ID
byte2：圈数
byte3-byte5：总时长，uint24百分秒
```

以下三种情况发送`0x12`：

- 成功定义运动员并发送成功`0xF0`之后。
- 名单内运动员通过8秒检测后。
- 响应`0x11`遍历运动员名单时。

第一名运动员定义完成后的初始信息示例，ID为1、圈数为0、总时长为0：

```text
AA 12 06 00 01 00 00 00 00 19 F9
```

### 8. `0x13` 运动员传输状态

发送方：STM32

Payload长度：1

```text
byte0：0x01表示开始传输，0x00表示结束传输
```

```text
开始：AA 13 01 01 15 F9
结束：AA 13 01 00 14 F9
```

### 9. `0x14` 检测到普通EPC

发送方：STM32

Payload长度：4

```text
byte0-byte3：EPC
```

该命令只用于既不在运动员名单、也不在黑名单中的EPC，不携带比赛时长。

```text
Payload：33 33 F3 37
完整包：AA 14 04 33 33 F3 37 A8 F9
```

### 10. `0xF0` 返回执行状态

发送方：STM32

Payload长度：2

```text
byte0：触发本次返回的原始命令ID
byte1：状态码
```

```text
0x00：成功
0x01：Payload长度错误
0x02：未知命令
0x03：RFID未初始化
0x04：RFID启动失败
0x05：RFID停止失败
0x06：EPC格式错误
0x07：RFID接收队列溢出
0x08：EPC去重表已满
0x09：LoRa发送失败
0x0A：当前状态不允许执行
0x0B：数据包校验和错误
0x0C：数据包尾错误
0x0D：Payload长度超过上限
0x0E：数据包接收超时
0x0F：RFID通信错误
```

`0x10`成功定义运动员时，先返回：

```text
AA F0 02 10 00 02 F9
```

随后立即发送该运动员的`0x12`。

## 四、EPC检测规则

检测优先级固定为：

```text
EPC黑名单 → 临时运动员名单 → 普通EPC 8秒临时列表
```

- 黑名单命中：完全静默。
- 运动员命中：距离该运动员上次有效检测不足8秒时静默；达到8秒后圈数加一、更新时间并发送`0x12`。
- 普通EPC：距离该EPC上次有效检测不足8秒时静默；达到8秒后发送`0x14`。
- 8秒静默不发送`0x12`、`0x14`或`0xF0`，也不更新上次有效检测时间。

临时运动员名单、EPC黑名单和普通EPC 8秒临时列表均固定为50条。固件只处理每张表中`enabled`为真的记录。所有数据仅存在于内存，设备重启或成功执行`0x02`后全部丢失。

## 五、命令响应关系

```text
APP发送0x01 → STM32返回0xF0
APP发送0x02 → STM32返回0xF0
APP发送0x03 → STM32返回0x04
APP发送0x10（运动员） → STM32返回0xF0成功，再返回0x12
APP发送0x10（非运动员） → STM32返回0xF0成功
APP发送0x11 → STM32返回0x13开始、多条0x12、0x13结束
STM32有效检测运动员 → STM32发送0x12
STM32有效检测普通EPC → STM32发送0x14
STM32发现协议或运行错误 → STM32发送0xF0
```
