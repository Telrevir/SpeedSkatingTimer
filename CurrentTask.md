# 当前任务

## 已完成

- 在`DetectOnly`分支保留开始、结束和状态查询。
- 增加`0x10`定义EPC、`0x11`查询运动员、`0x12`运动员信息、`0x13`传输状态和`0x14`普通EPC。
- 使用三张各50槽的固定内存表维护运动员、黑名单和普通EPC 8秒记录。
- 第一名运动员定义时圈数和总时长为0，后续运动员共用统一时间偏移量。
- 运动员和普通EPC分别执行8秒静默规则。
- 成功定义运动员时先返回`0xF0`成功，再立即返回`0x12`。
- `0x02`成功后清空所有临时比赛数据。
- 保持无TF卡、无数据库、无动态运动员存储。

## 待实机验证

- APP按`LORAProtocol-byte.md`完成`0x10`至`0x14`联调。
- 第一名和后续运动员定义后的初始`0x12`内容。
- 运动员与普通EPC在8秒边界前后的发送行为。
- `0x11`空名单和50人名单的开始、条目、结束序列。
- `0x02`后重新开始时，旧运动员和黑名单均不再生效。
