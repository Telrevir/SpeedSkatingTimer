# DetectOnly 固件设计

## 目标

DetectOnly 固件只负责开始检测、结束检测、查询状态、EPC检测上报和执行状态返回。运动员名单、圈数、排名、领滑和停止后的业务判断全部由APP处理。

## 模块边界

- `RFIDReader.h`：负责RFID硬件通信、EPC解析和接收队列，不处理8秒业务去重。
- `DetectionController.h`：维护停止/检测中状态、开始时间和100条EPC去重记录。
- `LoraManager.h`：只解析和发送`0x01`、`0x02`、`0x03`、`0x04`、`0x13`、`0xF0`协议。
- `SpeedSkatingTimer.ino`：初始化模块并转发控制命令和EPC事件。

## 状态行为

- 开机状态为已停止。
- `0x01`只在已停止状态生效。检测中重复发送时不改变时间、去重表或RFID状态，并返回状态不允许。
- `0x02`只在检测中状态生效。成功后停止RFID、清空队列和去重表并清零时间。
- `0x03`成功时只返回`0x04`。

## EPC处理

- EPC固定为4字节。
- 有效EPC通过`0x13`携带EPC和从开始起计算的3字节百分秒总时长。
- 同一EPC在上次有效检测后不足8秒时完全静默，不发协议、不打日志、不更新时间。
- 去重表固定100条。全部记录都在8秒保护期且没有可复用记录时，通过`0xF0`返回表满。
- 百分秒最大值固定为`0xFFFFFF`，达到后饱和，不回绕。

## 错误处理

协议长度、未知命令、包校验、包尾、RFID状态、EPC格式、队列溢出、去重表满和发送失败均使用`0xF0`状态码，并在固件终端输出对应中文原因。正常8秒重复检测不属于错误。

## 清理范围

主程序不再引用`AthleteManager.h`、`TimingLogic.h`或`StorageManager.h`，也不再初始化或访问TF卡。旧运动员、成绩、名单、领滑、绑定和存储协议从LoRa代码中完全删除。
