# LORA 字节协议重构计划

本文档记录 LoRa 字节规范协议的重构计划。目标是基于 `LORAProtocol-byte.md`，将 LoRa 通讯从当前文本协议逐步切换为字节协议，减少包长度，并保留可测试、可回退路径。



## 一、重构目标

当前 LoRa 通讯主要使用文本协议，数据包中包含字段名和分隔符，包长较大。

本次重构目标：

1. 使用字节规范协议进行 LoRa 通讯。
2. 压缩 LoRa 数据包长度。
3. 支持 ESP32 与 STM32 之间的字节命令收发。
4. 保留当前文本协议作为过渡期测试路径。
5. 通过串口调试命令模拟 LoRa 字节输入，降低测试成本。



## 二、协议常量整理

在 `LoraManager.h` 中定义字节协议常量：

包头：0xAA
包尾：0xF9
通用回复：0xF1
成功状态码：0x00
最大 Payload 长度：不超过 LoRa 单包限制，当前按 230 字节控制

命令 ID 应与 `LORAProtocol-byte.md` 保持一致，例如：

0x01 开启/关闭/重置比赛
0x02 返回运动员成绩
0x03 获取所有运动员成绩
0x04 所有运动员成绩传输状态
0x05 获取领滑运动员成绩
0x06 返回领滑运动员成绩
0x11 添加运动员
0x12 扫描添加运动员
0x13 编辑运动员
0x14 扫描编辑运动员
0x15 删除运动员
0x16 查询运动员信息
0x17 返回运动员信息
0x18 获取运动员列表
0x19 运动员列表传输状态
0xF1 通用命令回复



## 三、基础数据编码工具

新增内部工具函数，用于统一处理字节协议字段：

1. 写入 `uint16` 大端序。
2. 写入 `uint24` 百分秒。
3. 写入 4 字节 EPC。
4. 写入 UTF-8 姓名，格式为 `nameLen + nameBytes`。
5. 计算 checksum，规则为 `CMD + LEN + Payload` 的低 8 位。
6. 组包，格式为 `0xAA + CMD + LEN + Payload + CHECKSUM + 0xF9`。

时间字段在发送前由毫秒转换为百分秒：

centiseconds = (milliseconds + 5) / 10



## 四、字节发送函数

保留当前文本发送函数：

`send(const String& message)`

新增字节发送入口，例如：

`sendPacket(byte commandId, const byte* payload, byte payloadLength)`

或内部私有函数：

`sendBytePacket(...)`

该函数负责：

1. 校验 Payload 长度。
2. 生成完整字节包。
3. 计算 checksum。
4. 通过 `loraSerial.write()` 发送完整字节包。



## 五、发送侧数据改造

优先实现 STM32 到 ESP32 的发送包，因为这些数据包不涉及复杂命令执行。

优先实现：

1. `0x02` 返回运动员成绩。
2. `0x06` 返回领滑运动员成绩。
3. `0x17` 返回运动员信息。
4. `0x19` 运动员列表传输状态。
5. `0xF1` 通用命令回复。

当前文本发送函数先保留，新增字节版本，避免一次性破坏测试路径。



## 六、接收侧状态机

新增 LoRa 字节接收状态机。

状态流程：

1. 等待包头 `0xAA`。
2. 读取 `CMD`。
3. 读取 `LEN`。
4. 读取 Payload。
5. 读取 checksum。
6. 读取包尾 `0xF9`。
7. 校验 checksum。
8. 校验 Payload 长度。
9. 分发到命令处理函数。

异常处理：

1. 空闲状态读到非 `0xAA` 字节时直接丢弃。
2. 长度超过最大 Payload 时丢弃当前包。
3. checksum 错误时丢弃当前包。
4. 包尾错误时丢弃当前包，并等待下一个包头重新同步。



## 七、字节命令解析

实现 ESP32 到 STM32 的字节命令解析。

需要支持：

1. `0x01` 开启/关闭/重置比赛。
2. `0x03` 获取所有运动员成绩。
3. `0x05` 获取领滑运动员成绩。
4. `0x11` 添加运动员。
5. `0x12` 扫描添加运动员。
6. `0x13` 编辑运动员。
7. `0x14` 扫描编辑运动员。
8. `0x15` 删除运动员。
9. `0x16` 查询运动员信息。
10. `0x18` 获取运动员列表。

解析后尽量复用现有 `LoraCommand` 和 `handleLoraCommand()`，避免大改主流程。

如果某些字节命令无法自然映射到当前 `LoraCommand` 字段，应优先扩展 `LoraCommand`，而不是绕开主流程重复实现业务逻辑。



## 八、串口调试命令联动

与当前待办中的串口调试功能配合：

`LORA,<hex bytes>`

示例：

`LORA,AA 01 02 9F`

串口命令应将逗号后的十六进制文本解析为 byte 数据，并直接送入 LoRa 字节解析函数，用于模拟 LoRa 接收。

建议在字节协议接收状态机完成后，再实现该调试入口。



## 九、兼容策略

开发阶段保留两套协议：

1. 当前文本协议继续可用。
2. 字节协议新增并行入口。

推荐过渡方式：

1. 先完成字节接收和通用回复。
2. 再完成字节发送。
3. 实机测试稳定后，再决定是否默认切换发送侧为字节包。

不要在未完成测试前删除当前文本协议路径。



## 十、文档同步

实现过程中需要同步维护：

1. `LORAProtocol-byte.md`
2. `ARCHITECTURE.md`
3. `CurrentTask.md`

如果文本协议仍保留，应在架构文档中说明当前处于双协议过渡期。



## 十一、验证计划

建议按以下顺序测试：

1. 手动构造 `0x01` START/STOP/RESET 包。
2. 手动构造 `0x11` ADD 包。
3. 手动构造 `0x15` REMOVE 包。
4. 测试 STM32 返回 `0xF1`。
5. 测试比赛中发送 `0x02` 成绩包。
6. 测试运动员列表：`0x18 -> 0x19 -> 多条 0x17 -> 0x19`。
7. 测试 checksum 错误包是否拒绝。
8. 测试包尾错误或长度错误后是否能重新同步。

建议先从“接收字节命令 + 通用回复”开始做，再做成绩和列表发送。这样能最快验证协议闭环。
