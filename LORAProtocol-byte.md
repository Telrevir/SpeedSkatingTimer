# LORA 字节通讯协议

本文档记录 LoRa 透传串口中使用的字节协议。该协议用于后续替代当前文本协议，以压缩数据包长度并降低 LoRa 发送占用时间。



## 一、基础包格式

所有数据包统一使用以下结构：

包头 + 命令ID + 数据长度 + 数据 + 校验和 + 包尾

字段定义：

包头：0xAA
命令ID：1 字节
数据长度：1 字节，仅表示数据区 Payload 的长度
数据：0-230 字节，具体格式由命令ID决定
校验和：1 字节，建议使用 命令ID + 数据长度 + 数据区所有字节 的低 8 位
包尾：0xF9

示例：关闭比赛命令

0xAA 0x01 0x01 0x00 0x02 0xF9

含义：

0xAA：包头
0x01：命令ID，开启/关闭/重置比赛
0x01：数据长度，Payload 长度为 1 字节
0x00：关闭比赛
0x02：校验和，0x01 + 0x01 + 0x00 = 0x02
0xF9：包尾

多字节整数默认使用高位在前，即 Big Endian。



## 二、通用数据类型

### 1. 运动员 ID

运动员 ID 使用 2 字节无符号整数：

byte0：ID 高位
byte1：ID 低位

例如 ID 为 1：

0x00 0x01

### 2. EPC

EPC 固定使用 4 字节原始数据。

例如文本 EPC `3333F337` 对应：

0x33 0x33 0xF3 0x37

### 3. 运动员姓名

姓名使用 UTF-8 原始字节，并在姓名前增加 1 字节姓名长度：

nameLen + nameBytes

例如姓名 `张三` 的 UTF-8 数据长度为 6 字节：

0x06 0xE5 0xBC 0xA0 0xE4 0xB8 0x89

所有同时包含 EPC 和姓名的数据结构，均采用 EPC 在前、姓名在后的顺序：

EPC(4字节) + nameLen(1字节) + nameBytes(N字节)

### 4. 时间

比赛时间使用 3 字节百分秒表示，单位为 0.01 秒，即 10 毫秒。

byte0：百分秒高位
byte1：百分秒中位
byte2：百分秒低位

例如 `10分26秒980毫秒`：

总毫秒 = 626980 ms
百分秒 = 626980 / 10 = 62698 = 0x00F4EA
存储为：0x00 0xF4 0xEA

发送前建议由毫秒转换为百分秒：

centiseconds = (milliseconds + 5) / 10

这样可以对毫秒进行四舍五入，避免直接截断导致显示时间偏小。

### 5. 比赛状态

比赛状态使用 1 字节表示：

byte0：0x00 未开始
byte0：0x01 等待运动员
byte0：0x02 进行中
byte0：0x03 领滑结束

默认状态为 `0x00` 未开始。

### 6. 运动员排名

运动员排名使用 1 字节无符号整数表示：

0x00：未排名或无有效成绩
0x01：第 1 名
0x02：第 2 名

以此类推，最大支持到 `0xFF`，即第 255 名。



## 三、命令列表

### 1. 赛道功能

#### 0x01 开启/关闭/重置比赛

发送方：ESP32

功能：启动、关闭或重置比赛。

动作含义：

0x01 开启比赛：STM32 进入等待运动员状态并开启 RFID。
0x00 关闭比赛：存在有效领滑运动员时进入领滑结束状态，记录当前领滑圈数作为目标圈数；达到目标圈数的运动员成绩会被锁定。领滑结束超过五分钟后，STM32 彻底停止计时并关闭 RFID。
0x02 重置比赛：STM32 回到未开始状态，彻底停止计时、关闭 RFID 并清空比赛运行数据。

数据格式：

byte0：0x01 开启比赛，0x00 关闭比赛，0x02 重置比赛

#### 0x02 返回运动员成绩

发送方：STM32

功能：比赛开始后，STM32 检测到运动员时向 ESP32 发送该运动员成绩。

数据格式：

byte0-byte1：运动员 ID
byte2：圈数
byte3-byte5：本圈用时，uint24 百分秒
byte6-byte8：总用时，uint24 百分秒
byte9：运动员排名

#### 0x03 获取所有运动员成绩

发送方：ESP32

功能：向 STM32 申请获取所有运动员成绩。STM32 接收后发送 `0x04` 开始传输，每条成绩使用 `0x02` 发送一次，最后发送 `0x04` 结束传输。

数据格式：无

#### 0x04 所有运动员成绩传输状态

发送方：STM32

功能：提示所有运动员成绩的传输进度。

数据格式：

byte0：0x01 开始传输，0x00 结束传输

#### 0x05 获取领滑运动员成绩

发送方：ESP32

功能：向 STM32 申请获取领滑运动员成绩。STM32 接收后返回 `0x06`。

数据格式：无

#### 0x06 返回领滑运动员成绩

发送方：STM32

功能：向 ESP32 返回领滑运动员成绩。

数据格式：

byte0-byte1：领滑运动员 ID
byte2：圈数
byte3-byte5：总用时，uint24 百分秒

#### 0x07 获取比赛状态

发送方：ESP32

功能：向 STM32 申请获取当前比赛状态。STM32 接收后返回 `0x08`。

数据格式：无

示例：

0xAA 0x07 0x00 0x07 0xF9

#### 0x08 返回比赛状态

发送方：STM32

功能：向 ESP32 返回当前比赛状态。该命令用于响应 `0x07`，也可在比赛状态变化后由 STM32 主动发送。

数据格式：

byte0：比赛状态

状态取值：

0x00：未开始
0x01：等待运动员
0x02：进行中
0x03：领滑结束

示例：返回进行中

0xAA 0x08 0x01 0x02 0x0B 0xF9

### 2. 运动员数据管理

#### 0x11 添加运动员

发送方：ESP32

功能：向 STM32 手动添加新运动员。运动员 ID 由 STM32 自动生成。

数据格式：

byte0-byte3：EPC
byte4：姓名长度 nameLen
byte5-byteN：姓名 UTF-8 原始字节

#### 0x12 扫描添加运动员

发送方：ESP32

功能：向 STM32 发送新运动员姓名并开启扫描。STM32 会将第一个扫描到的未绑定 EPC 作为新运动员的 EPC 进行添加。

数据格式：

byte0：姓名长度 nameLen
byte1-byteN：姓名 UTF-8 原始字节

#### 0x13 编辑运动员

发送方：ESP32

功能：向 STM32 发送需要编辑的运动员 ID、EPC 和姓名。若运动员已存在，则更新姓名并确保该 EPC 已绑定到该运动员；若运动员不存在，则按该 ID 新增运动员并创建第一条 EPC 绑定。

数据格式：

byte0-byte1：运动员 ID
byte2-byte5：EPC
byte6：姓名长度 nameLen
byte7-byteN：姓名 UTF-8 原始字节

#### 0x14 扫描编辑运动员

发送方：ESP32

功能：向 STM32 发送需要编辑的运动员 ID 和姓名。STM32 会将第一个扫描到的未绑定 EPC 绑定到该运动员。

数据格式：

byte0-byte1：运动员 ID
byte2：姓名长度 nameLen
byte3-byteN：姓名 UTF-8 原始字节

#### 0x15 删除运动员

发送方：ESP32

功能：向 STM32 发送需要删除的运动员 ID。

数据格式：

byte0-byte1：运动员 ID

#### 0x16 查询运动员信息

发送方：ESP32

功能：向 STM32 发送需要查询的运动员 ID。

数据格式：

byte0-byte1：运动员 ID

#### 0x17 返回运动员信息

发送方：STM32

功能：向 ESP32 返回查询到的运动员信息，也可用于运动员列表传输中的单条运动员数据。

数据格式：

byte0-byte1：运动员 ID
byte2-byte5：EPC
byte6：姓名长度 nameLen
byte7-byteN：姓名 UTF-8 原始字节

#### 0x18 获取运动员列表

发送方：ESP32

功能：向 STM32 申请获取运动员列表。STM32 接收后发送 `0x19` 开始传输，每条运动员信息使用 `0x17` 发送一次，最后发送 `0x19` 结束传输。

数据格式：无

#### 0x19 运动员列表传输状态

发送方：STM32

功能：提示运动员列表的传输进度。

数据格式：

byte0：0x01 开始传输，0x00 结束传输

#### 0x1A 绑定 EPC 到运动员

发送方：ESP32

功能：将指定 EPC 绑定到指定运动员。该命令只处理标签绑定，不修改运动员姓名。

数据格式：

byte0-byte1：运动员 ID
byte2-byte5：EPC

#### 0x1B 扫描绑定 EPC 到运动员

发送方：ESP32

功能：发送运动员 ID 并进入扫描绑定流程。STM32 会将第一个扫描到的未绑定 EPC 绑定到该运动员。

当前状态：预留，待扫描绑定流程实现。当前固件收到该命令时返回 `0xF1` 状态码 `0x06`。

数据格式：

byte0-byte1：运动员 ID

#### 0x1C 解绑 EPC

发送方：ESP32

功能：解除指定运动员与指定 EPC 的绑定关系。

数据格式：

byte0-byte1：运动员 ID
byte2-byte5：EPC

#### 0x1D 获取运动员 EPC 列表

发送方：ESP32

功能：获取指定运动员绑定的所有 EPC。

数据格式：

byte0-byte1：运动员 ID

#### 0x1E 返回运动员 EPC 列表

发送方：STM32

功能：返回指定运动员绑定的 EPC 列表。

数据格式：

byte0-byte1：运动员 ID
byte2：EPC 数量
byte3-byteN：EPC 列表，每个 EPC 固定 4 字节

### 3. 通用回复

#### 0xF1 通用命令回复

发送方：STM32

功能：向 ESP32 返回命令执行结果。主要用于添加、扫描添加、编辑、扫描编辑、删除等需要确认执行结果的命令。

数据格式：

byte0：原命令 ID
byte1：状态码，0x00 表示成功，其余均表示错误

建议状态码：

0x00：成功
0x01：数据格式错误
0x02：EPC 已绑定
0x03：未找到运动员
0x04：等待扫描超时
0x05：存储失败
0x06：当前状态不允许执行
0x08：ID 已存在
0x0B：该运动员未绑定该 EPC
0x0C：EPC 绑定数量已达上限



## 四、SCAN 设计说明

当前字节协议不再保留独立的 `SCAN` 命令。

扫描能力被整合进以下命令：

0x12 扫描添加运动员
0x14 扫描编辑运动员

这样 ESP32 不需要维护“进入扫描、等待 EPC、再判断添加或编辑”的中间状态。STM32 负责 RFID 扫描和运动员数据绑定，ESP32 只需要发送业务目标命令并等待 `0xF1` 通用回复。
