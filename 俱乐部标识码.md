# 功能概述
能将运动员每次的比赛成绩保存下来，并通过APP进行查看。




# 功能具体描述：
## 1. 本地文件操作
添加新路径SavedScore，在其中添加一个csv文件：RaceRecordManager
该文件用于维护SavedScore下保存的比赛记录，信息包括：记录ID，记录文件名，记录时间







在每次开启比赛后，将当前比赛的运动员成绩以csv格式保存到内存卡指定路径SavedScore/List下，默认名称为YYYY-MM-DD-HH-mm-ss。
同时更新RaceRecordManager,ID为RaceRecordManager最新ID自增1，记录时间为比赛开始时间。
在更新运动员成绩时，将该运动员的姓名，EPC，圈数，单圈用时，总用时写入到csv行内。
按下重置后，如果没有任何运动员成绩，则自动删除该文件。

列表最多维护65535个文件，
