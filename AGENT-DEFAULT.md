# AGENT 行动准则

该文档用于约束 AI/自动化编码助手在项目中的阅读、计划、编码、文档维护和总结行为。具体项目可在此基础上另行补充项目专属规则。



## 一. 基本规则

### 1. 优先阅读项目规则

每次阅读、计划、编写或修改代码前，应优先检查项目中是否存在以下规则或说明文档：

```text
AGENT.md
AGENTS.md
CurrentTask.md
ARCHITECTURE.md
README.md
```

如果存在，应根据其内容行动。

### 2. 书面语言

项目文档、代码注释、调试提示的语言应遵循项目既有约定。

如果项目没有明确约定，默认使用用户当前主要交流语言。

涉及二进制协议、命令名、字段名、宏名、函数名、类名、文件名时，应按代码和协议需要使用英文或 ASCII。

### 3. 文档格式

编写和整理 Markdown 文档时，标题和其自身正文之间保持一行空行。一级标题、二级标题所属正文结束后，如果后面还有其他标题，应在正文和下一个标题之间保留三行空行。三级及以下标题所属正文结束后，与下一个标题之间保留一行空行。

如果用户提出的格式规则存在歧义，例如“标题间隔”没有明确指向标题后还是正文后，应先提出明确定义再修改文档。

任务文档、架构文档、协议文档和 README 应保持职责清晰，避免多个文档长期重复维护同一大段内容。

短字段格式、协议字段和命令格式可直接使用纯文本行表达；仅在需要展示代码、命令或多行固定格式示例时使用代码块。

### 4. 平台与依赖

编写代码前应确认当前项目的运行平台、框架、语言版本和可用依赖。

不要未经确认就引入平台专属能力、第三方库或需要额外安装的依赖。

如确需引入新库、工具或平台能力，应先说明理由、影响范围和替代方案。



## 二. 编程行为准则

### 1. 精准修改

只修改必要部分，保持代码整洁性。

不要大规模重构无关代码，不要无故触动核心逻辑。

如修改会影响核心链路、公共接口、数据格式或外部协议，应先说明影响范围。

### 2. 简单优先

使用最少的代码解决问题，避免不必要的复杂性。

优先采用项目已有风格、已有工具和已有抽象，不轻易引入新架构。

### 3. 保持思考

明确假设内容，不要隐藏困惑。

如果用户提出的计划存在问题、隐患或更优替代方案，应主动提出，并说明原因。

如果信息不足且无法通过项目内容确认，应提出简短问题；如果可以基于合理假设继续推进，应说明假设后继续。

### 4. 保持易读

依据项目已有风格命名。如果没有明确风格，默认使用该语言/框架的常见命名方式。

新增代码应保持清晰、局部、可维护。

关键位置应留下简短注释。注释应解释原因、边界、兼容性或风险，不要重复代码表面含义。

### 5. 保留可回退路径

对于高风险修改，如硬件通信、协议切换、存储方案、数据迁移、核心调度逻辑等，应优先保留旧路径或用注释标记旧方案，除非用户明确要求删除。

建议标记示例：

```cpp
// LEGACY_PATH:
// 旧实现保留，用于回退测试。
```

### 6. 尊重现场改动

项目可能存在用户手工调试代码、临时日志、现场测试注释或未提交改动。

不要擅自删除或回滚这些内容，除非它们与当前任务冲突，并且已经说明原因。

### 7. 测试状态

如果用户明确说明“测试完成后再标记完成”，则在用户确认前不得将任务移入已完成列表。

如果修改尚未经过编译、自动测试或实机测试，应在总结中明确说明。



## 三. 文档维护

### 1. 任务文档

如果项目存在任务文档，例如：

```text
CurrentTask.md
TODO.md
TASKS.md
```

则在添加、完成、取消计划时应同步维护。

任务文档应只记录任务状态、待办和决策，不应堆叠大段系统架构说明。

### 2. 架构文档

如果项目存在架构文档，例如：

```text
ARCHITECTURE.md
DESIGN.md
```

则在实际架构、模块职责、数据流或核心边界发生变化时应同步更新。

### 3. 协议与接口文档

如果项目存在协议、接口、API 或数据格式文档，则在以下内容变化时必须更新：

```text
命令格式
字段含义
返回格式
错误码
数据包格式
文件格式
兼容性说明
```

### 4. 文档分工

应保持文档职责清晰：

```text
任务文档：记录当前做什么、完成了什么、取消了什么
架构文档：记录系统是什么、模块如何协作
协议文档：记录外部通信和数据格式
README：记录项目入口、使用方式、构建方式
```

避免多个文档长期重复维护同一大段内容。



## 四. 修改流程

### 1. 修改前

在动手前应先理解相关代码和文档。

对影响范围较大的任务，应先列出修改计划供用户确认。

### 2. 修改中

优先小步修改。

避免一次性重写大文件。

保留项目既有风格和接口，除非修改接口是任务目标。

### 3. 修改后

完成后应进行适当检查，例如：

```text
静态搜索
编译
单元测试
集成测试
实机测试
文档一致性检查
```

如果无法执行某项检查，应在总结中说明。



## 五. 总结要求

完成修改后，应简要说明：

1. 修改了哪些文件。
2. 改动的核心行为。
3. 是否更新了相关文档。
4. 是否做过编译、测试或实机验证。
5. 是否存在风险、限制或待验证点。

总结应简洁、明确，不应夸大已验证范围。
