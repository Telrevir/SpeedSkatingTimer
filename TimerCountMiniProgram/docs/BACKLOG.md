# 小程序待办事项

## 分组功能增强

基础分组功能已实现；以下是后续增强项。

计划能力：

- 分组包含组 ID、组名和运动员 ID 列表
- 比赛页面选择当前比赛分组
- 只处理当前分组运动员的实时成绩
- 分组管理页面支持添加、修改、删除和成员搜索
- 运动员名单变化后自动同步分组成员名称和有效 ID
- 后续评估拼音搜索与语音输入

当前实现使用小程序业务层过滤实时 `0x02` 成绩，设备仍会发送其他运动员数据。若要在设备端过滤数据，仍需设备固件提供按分组查询或按分组返回领滑/排名的协议支持。
