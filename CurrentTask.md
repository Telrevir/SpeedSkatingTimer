# 当前任务

## 已完成

- 创建`DetectOnly`分支。
- 定义仅包含`0x01`、`0x02`、`0x03`、`0x04`、`0x13`、`0xF0`的LoRa协议。
- 将固件精简为开始、结束、状态查询、EPC检测和状态返回。
- 使用100条固定容量记录实现8秒EPC去重。
- 使用3字节大端序百分秒上报从开始命令起计算的总时长。
- 删除本地存储、运动员名单、圈数、排名和领滑逻辑。

## 待实机验证

- STM32与RFID模块连续轮询稳定性。
- APP与LoRa字节协议联调。
- 同一EPC在8秒边界前后的上报行为。
- 100个不同EPC并发检测和去重表满错误返回。
